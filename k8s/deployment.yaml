apiVersion: apps/v1
kind: Deployment
metadata:
  name: flappy-bird-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flappy-bird
  template:
    metadata:
      labels:
        app: flappy-bird
    spec:
      containers:
      - name: flappy-bird-container
        image: ziyadtarek99/flappy-bird-game:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
